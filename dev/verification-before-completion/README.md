# verification-before-completion (Dev-only)

This directory contains development-only artifacts for the `verification-before-completion` skill.

## Quick smoke

From the repo root:

```sh
python3 dev/verification-before-completion/run_smoke.py
```

The smoke validates the checked-in verification-first gate and its core evidence-before-claims language.

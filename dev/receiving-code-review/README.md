# receiving-code-review (Dev-only)

This directory contains development-only artifacts for the `receiving-code-review` skill.

## Quick smoke

From the repo root:

```sh
python3 dev/receiving-code-review/run_smoke.py
```

The smoke validates the checked-in review-reception contract for technical verification, bounded pushback, and GitHub thread reply behavior.
